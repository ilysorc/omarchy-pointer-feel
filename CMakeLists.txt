cmake_minimum_required(VERSION 3.25)
project(omarchy_pointer_feel VERSION 0.4.0 LANGUAGES CXX)

# Offline reference engines; the optional module requires matching Hyprland headers.
set(WINDOWS_REFERENCE "${CMAKE_CURRENT_SOURCE_DIR}/vendor/windows-pointer-linux")
add_library(pointer_feel_windows STATIC "${WINDOWS_REFERENCE}/src/engine.cpp")
target_include_directories(pointer_feel_windows PUBLIC "${WINDOWS_REFERENCE}/include")
target_compile_features(pointer_feel_windows PUBLIC cxx_std_23)
target_compile_options(pointer_feel_windows PRIVATE -Wall -Wextra -Wpedantic)
set_target_properties(pointer_feel_windows PROPERTIES POSITION_INDEPENDENT_CODE ON)

add_executable(pointer-feel-replay "${WINDOWS_REFERENCE}/tools/replay.cpp")
target_link_libraries(pointer-feel-replay PRIVATE pointer_feel_windows)

find_package(Python3 REQUIRED COMPONENTS Interpreter)
file(GLOB MAC_REFERENCE_INPUTS CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/vendor/libpointing-sequoia/*")
add_custom_command(OUTPUT "${CMAKE_CURRENT_BINARY_DIR}/mac_tables.hpp"
  COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tools/generate-mac-tables.py" "${CMAKE_CURRENT_BINARY_DIR}/mac_tables.hpp"
  DEPENDS tools/generate-mac-tables.py ${MAC_REFERENCE_INPUTS})
add_library(pointer_feel_mac STATIC src/mac/engine.cpp "${CMAKE_CURRENT_BINARY_DIR}/mac_tables.hpp")
target_include_directories(pointer_feel_mac PUBLIC src/mac PRIVATE "${CMAKE_CURRENT_BINARY_DIR}")
target_compile_features(pointer_feel_mac PUBLIC cxx_std_23)
target_compile_options(pointer_feel_mac PRIVATE -Wall -Wextra -Wpedantic)
set_target_properties(pointer_feel_mac PROPERTIES POSITION_INDEPENDENT_CODE ON)
add_executable(pointer-feel-mac-replay tools/mac-replay.cpp)
target_link_libraries(pointer-feel-mac-replay PRIVATE pointer_feel_mac)

option(POINTER_FEEL_BUILD_PLUGIN "Build both Hyprland pointer modules" OFF)
if(POINTER_FEEL_BUILD_PLUGIN)
  find_package(PkgConfig REQUIRED)
  pkg_check_modules(HYPRLAND REQUIRED IMPORTED_TARGET hyprland)
  add_library(windows-pointer-linux MODULE "${WINDOWS_REFERENCE}/src/plugin.cpp")
  target_link_libraries(windows-pointer-linux PRIVATE pointer_feel_windows PkgConfig::HYPRLAND)
  target_compile_options(windows-pointer-linux PRIVATE -Wall -Wextra -Wpedantic)
  target_compile_definitions(windows-pointer-linux PRIVATE WINDOWS_POINTER_VERSION="1.0.0")
  set_target_properties(windows-pointer-linux PROPERTIES PREFIX "")
  add_library(pointer-feel-mac MODULE src/mac_plugin.cpp)
  target_link_libraries(pointer-feel-mac PRIVATE pointer_feel_mac PkgConfig::HYPRLAND)
  target_compile_options(pointer-feel-mac PRIVATE -Wall -Wextra -Wpedantic)
  if(CMAKE_CXX_COMPILER_ID STREQUAL "GNU")
    target_compile_options(pointer-feel-mac PRIVATE -fno-gnu-unique)
    target_compile_options(windows-pointer-linux PRIVATE -fno-gnu-unique)
  endif()
  set_target_properties(pointer-feel-mac PROPERTIES PREFIX "")
endif()

include(CTest)
if(BUILD_TESTING)
  find_package(GTest REQUIRED)
  add_executable(windows_reference_tests
    "${WINDOWS_REFERENCE}/tests/engine_test.cpp"
    "${WINDOWS_REFERENCE}/tests/model_test.cpp"
    "${WINDOWS_REFERENCE}/tests/scenario_test.cpp")
  target_link_libraries(windows_reference_tests PRIVATE pointer_feel_windows GTest::gtest_main)
  include(GoogleTest)
  gtest_discover_tests(windows_reference_tests)
  add_executable(mac_reference_tests tests/mac/engine_test.cpp)
  target_link_libraries(mac_reference_tests PRIVATE pointer_feel_mac GTest::gtest_main)
  gtest_discover_tests(mac_reference_tests)
endif()
